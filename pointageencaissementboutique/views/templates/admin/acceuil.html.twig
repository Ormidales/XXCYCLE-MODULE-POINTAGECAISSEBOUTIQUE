{% extends '@PrestaShop/Admin/layout.html.twig' %}

{% block content %}

<script src="https://cdn.tailwindcss.com"></script>

<div class="w-full h-[20vh] flex items-center justify-center">

    <div class="w-full px-3 py-2 bg-gray-600 mb-4 rounded-lg">
        <p class="text-white text-[18px] font-bold mb-2">Liste des encaissements</p>
        {{ form_start(formSelect) }}
            {{ form_row(formSelect.form_date) }}
            {{ form_row(formSelect.form_type) }}
            {{ form_row(formSelect.form_mode) }}
            {{ form_row(formSelect.form_submit) }}
        {{ form_end(formSelect) }}
    </div>

</div>

{% endblock %}